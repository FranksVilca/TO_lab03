#include "Mamifero.h"

int main()
{
    Dog myDog;       // Crear un objeto Dog
    myDog.Move();    // Llamar a Move en Dog
    myDog.Speak();   // Llamar a Speak en Dog
    myDog.WagTail(); // Llamar a WagTail en Dog
    return 0;
}
